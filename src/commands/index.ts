import * as ping from './utility/ping'
import * as whoami from './utility/whoami'
import * as startValheim from './aws/start-valheim'
import * as unknown from './utility/unknown'

export const commands = {
  ping,
  startValheim,
  unknown,
  whoami,
}
